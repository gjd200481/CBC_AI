# CBC_AI 项目改进路线图

**创建时间**: 2026-06-15  
**当前阶段**: 技术验证完成，发现关键物理问题，进入二次改进阶段

---

## 🚨 核心发现：当前多平面数据存在致命缺陷

### 问题描述

经过与 Hou 2019 / Xie 2024 文献详细对照，以及对当前 `multiplane_0_-0.03/05/07` 数据集的验证，发现：

**当前"焦平面"和"焦前平面"两通道几乎完全相同** (`np.allclose=True`，差异仅 1e-19 ~ 1e-21)

### 根本原因

当前 `propagation.py:multiplane_far_field_intensity()` 的实现方式：

```python
# 当前错误方式 (Line 89-96)
if dist == 0:
    far_field = np.fft.fftshift(np.fft.fft2(near_field))
else:
    propagated = propagate_fn(near_field, wavelength, dist, pixel_size)
    far_field = np.fft.fftshift(np.fft.fft2(propagated))  # ❌ 再次 FFT

intensity = np.abs(far_field) ** 2
```

**问题**：自由空间传播在频域主要乘以单位模相位因子 `exp(j*kz*distance)`，再对结果做 FFT 取强度，两个平面的强度几乎不变。

**正确方式**：应该加入透镜相位，传播到 `z=f` 和 `z=f+Δz` 探测面后直接取 `|U|^2`，不要"传播后再 FFT"。参考 Hou 2019 配置：`f=20m`，焦前 `0.4-0.6m`。

---

## 🎯 优先级排序的改进路线

根据对 Hou 2019、Mills 2022、Xie 2024 的系统对照和当前项目状态，改进路线按优先级排序如下：

### Priority 1 - 修正物理仿真 ✅ 已完成

**目标**: 修正多平面仿真，生成真正的焦平面/焦前平面探测强度

**任务**:
1. ✅ 在 `propagation.py` 中新增 `lens_focus_multiplane()` 函数
2. ✅ 实现透镜相位 + 传播到探测面 + 取强度，避免"传播后再FFT"
3. ✅ 参考参数：焦距 `f=0.5-2.0m`（可调），焦前距离 `Δz` 扫描范围 `0.01-0.1m`
4. ✅ 重新生成 `multiplane_corrected_*` 数据集
5. ✅ 用新数据验证两通道确实有显著差异

**完成结果**:
- ✅ 新数据集两通道 `np.allclose=False`
- ✅ 最大差异 **0.42**（归一化强度），远大于目标 0.01
- ✅ 平均差异 0.0008，前10样本无一退化相同
- ✅ 32样本 smoke 验证通过
- 🔄 10k 完整数据集生成中（后台运行）

**新增文件**:
- `simulation/common/propagation_corrected.py` - 修正后的传播模块
- `simulation/static/generate_corrected_multiplane.py` - 修正后的数据生成脚本
- `scripts/verify_multiplane_data_issue.py` - 数据验证脚本
- `scripts/visualize_corrected_vs_old.py` - 可视化对比脚本
- `result/figures/multiplane_correction_comparison.png` - 修正前后对比图

**实际耗时**: ~1小时（含问题验证、代码实现、smoke测试）

---

### Priority 2 - 离焦距离与光束间距联合扫描 🔄 准备中

**目标**: 找到信息量最大的非焦平面配置，而不是固定 7cm

**任务**:
1. 修正仿真后，扫描 `Δz = [0.01, 0.03, 0.05, 0.07, 0.1]m`
2. 同时扫描 `beam_distance/waist = [2.0, 2.5, 3.0, 3.5, 4.0]`（当前固定为 3.0）
3. 对每个配置训练 10-15 epoch smoke 模型
4. 评估 RMSE、Strehl、主瓣能量和噪声鲁棒性
5. 选择最佳配置进入主实验

**依赖**: ✅ Priority 1 完成

**下一步行动**: 等待 10k 数据生成完成后，先用 `f=1.0, Δz=-0.05` 配置快速训练验证，然后开始配置扫描

**完成标准**:
- 输出配置矩阵热图（RMSE / Strehl）
- 给出推荐配置和理由
- 结论可引用或作为消融实验

**预估耗时**: 6-8小时（含训练）

---

### Priority 3 - 数据规模扩展（50k-100k）

**目标**: 判断数据规模是否仍是瓶颈

**任务**:
1. 使用 Priority 2 最佳配置，生成 50k 样本
2. 在 Cycle42 双分支融合模型上训练 30-50 epoch
3. 与当前 10k 数据结果对比
4. 若收益显著，进一步生成 100k 数据集

**依赖**: Priority 1、2 完成

**完成标准**:
- 明确回答"当前 RMSE/Strehl 是否主要受限于数据规模"
- 若收益 < 5%，则当前 10k 数据已充分
- 若收益 > 10%，则 Hou 2019 的 100k 数据策略值得复现

**预估耗时**: 4-6小时（含数据生成）

---

### Priority 4 - 周期约束与单位圆正则强化

**目标**: 减少 sin/cos 输出偏离 `sin²+cos²=1`

**任务**:
1. 在当前最佳模型上增强 `lambda_unit` 损失
2. 尝试在输出层加入显式归一化：`output / norm(output)`
3. 添加周期边界损失：减少 `φ=π` 附近的预测跳变
4. 与当前 Cycle42/44 对比

**依赖**: Priority 1、2 完成（使用修正数据）

**完成标准**:
- 输出分布更集中在单位圆上
- 相位 RMSE 和 Strehl 至少保持不下降
- 若有提升，则写入主模型；若无效，作为负结果保留

**预估耗时**: 3-4小时

---

### Priority 5 - 混合物理优化：网络预测 + SPGD 微调

**目标**: 用网络预测作为初始化，接 3-10 步物理优化，专门拉高 Strehl/主瓣能量

**任务**:
1. 实现 SPGD（随机并行梯度下降）或简单梯度上升优化器
2. 以 CNN 预测相位为起点，优化目标为最大化远场峰值强度或 Strehl
3. 限制微调步数（3-10步），保持推理速度优势
4. 在 256 样本测试集上评估补偿指标
5. 对比 Cycle42 纯 CNN 和 "Cycle42 + SPGD 微调"

**依赖**: Priority 1、2 完成

**完成标准**:
- Strehl 提升 > 5%，或主瓣能量提升 > 3%
- 推理时间仍在 50ms 内（含微调）
- 若有效，写入论文作为混合方法创新点

**预估耗时**: 5-6小时

---

### Priority 6 - 噪声增强策略优化

**目标**: 解决物理约束模型对噪声不稳定的问题

**任务**:
1. 在修正后的多平面数据上重新训练 Cycle44 noise-augmented 模型
2. 尝试更激进的噪声增强：`σ_max=0.01` 或 `0.02`
3. 尝试"干净 + 噪声"混合训练策略：前 50% epoch 干净，后 50% 加噪声
4. 输出统一鲁棒性曲线：`σ=0, 0.001, 0.002, 0.005, 0.01, 0.02, 0.03`

**依赖**: Priority 1 完成

**完成标准**:
- 在 `σ≥0.01` 时，Strehl/主瓣能量优于当前 Cycle42
- 在 `σ=0` 时，性能不低于 Cycle42 `-10%`
- 给出"干净性能 vs 鲁棒性"权衡曲线

**预估耗时**: 4-5小时

---

### Priority 7 - 双分支架构改进

**目标**: 在修正后的真实多平面数据上，验证 Cycle42 双分支融合是否仍有效

**任务**:
1. 用修正后的多平面数据重新训练 Cycle42 模型
2. 与简单通道堆叠（Cycle41）公平对比
3. 做 IG+Grad-CAM attribution，检查焦前分支是否真正提供局部线索
4. 若 Cycle42 优势消失，说明之前收益可能来自"伪多平面"的意外正则效果
5. 若 Cycle42 优势增强，说明真实多平面数据 + 双分支融合是正确方向

**依赖**: Priority 1 完成

**完成标准**:
- 明确回答"真实多平面 + 双分支融合是否优于单焦平面"
- Attribution 图显示焦前分支是否有独特贡献
- 更新论文 Results 和 Discussion 章节

**预估耗时**: 5-6小时

---

## 📊 当前状态与新发现的影响

### 已完成的 Cycle 42-44 结果的有效性

#### ✅ 仍然有效的结论

1. **双分支门控融合架构优于简单通道堆叠**（参数 5.77M vs 11.34M）
2. **动态噪声增强在 σ≥0.005 时显著提升鲁棒性**
3. **IG+Grad-CAM 显示模型确实学到了物理一致的特征**
4. **补偿质量损失 warmup 策略有效**
5. **单位圆约束、六边形对称增强等负结果**

**原因**: 这些结论主要依赖网络架构、训练策略和噪声处理，与输入数据的"焦平面vs焦前"具体物理含义关系不大。

#### ⚠️ 需要重新验证的结论

1. **"焦前图像提供更局部的相位线索"** - 当前两通道相同，无法验证
2. **"多平面输入优于单焦平面"** - 需要用真实多平面数据重新对比
3. **"焦平面能量 48.4%，焦前能量 51.6%"** - attribution 比例在真实数据下可能完全不同
4. **当前 RMSE 0.892 rad / Strehl 0.683 的上限** - 真实多平面数据可能带来进一步提升

**影响**: 论文 Results 4.4节（多平面融合）和 Discussion 5.2节（架构设计）需要根据修正后数据重写。

---

## 🎯 短期行动计划（本周）

### Day 1-2: Priority 1 - 修正多平面仿真 ✅ 进行中

- [x] 验证当前数据问题（已确认）
- [ ] 实现正确的透镜焦平面 + 离焦探测模型
- [ ] 生成小规模 smoke 数据集验证两通道有差异
- [ ] 生成 10k 修正数据集

### Day 3-4: Priority 2 - 配置扫描

- [ ] 离焦距离扫描：5个配置
- [ ] 光束间距扫描：5个配置
- [ ] 选出最佳配置

### Day 5-6: Priority 7 - 重新验证双分支融合

- [ ] 用修正数据重新训练 Cycle42
- [ ] 对比修正前后的性能和 attribution
- [ ] 更新论文相关章节

### Day 7: Priority 3 或 Priority 5

- 若修正后性能显著提升 → Priority 3（扩展到 50k）
- 若修正后性能持平或下降 → Priority 5（混合物理优化）

---

## 📈 预期收益

### 乐观场景（修正后性能提升）

- RMSE 降至 `0.80-0.85 rad`（当前 0.892）
- Strehl 提升至 `0.70-0.75`（当前 0.683）
- 焦前图像确实提供更强相位线索
- 论文创新点"多平面融合"完全成立

### 现实场景（修正后性能持平）

- RMSE 保持 `0.85-0.90 rad`
- Strehl 保持 `0.65-0.70`
- 多平面收益有限，但物理模型更正确
- 论文重点转向"双分支架构 + 噪声鲁棒性"

### 保守场景（修正后性能下降）

- RMSE 上升至 `0.95-1.00 rad`
- 说明之前"伪多平面"意外提供了某种正则效果
- 需要 Priority 5（混合物理优化）来弥补性能
- 论文诚实讨论"物理正确性 vs 性能权衡"

**无论哪种场景，修正物理仿真都是必须的，因为当前数据与文献描述不符。**

---

## 🔗 相关文档

- 当前项目状态：[PROJECT_STATUS.md](PROJECT_STATUS.md)
- 模型构建指南：[docs/MODEL_CONSTRUCTION_GUIDE.md](docs/MODEL_CONSTRUCTION_GUIDE.md)
- 论文草稿：[paper/results_section_draft.md](paper/results_section_draft.md)
- 最终完成报告：[docs/FINAL_COMPLETION_REPORT.md](docs/FINAL_COMPLETION_REPORT.md)

---

## 📝 更新日志

- **2026-06-15 16:30**: ✅ Priority 1 完成！修正后数据两通道差异达 0.42，10k 数据集生成中
- **2026-06-15 14:00**: 创建改进路线图，发现并确认多平面数据退化问题，开始 Priority 1 修正工作
