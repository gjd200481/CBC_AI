# 项目目标修订说明：从双光束到 7 光束相干合成

## 修订背景

项目早期以双光束相干合成为对象，完成了远场光强生成、CNN 相位反演、傅里叶光学物理一致性损失、噪声鲁棒性和振幅失配实验。这条路线已经证明了方法在低维相位反演任务中的可行性。

但双光束系统只有一个相对相位，控制维度较低，难以充分体现多路相干合成系统中的高维相位耦合和多通道反演难度。因此，后续主目标修订为 7 光束相干合成相位误差反演。

## 新主目标

建立一种面向 7 光束相干合成系统的基于傅里叶光学约束 CNN 的相位误差反演方法。

系统结构：

```text
中心参考光束：beam_0, phase = 0
外圈六边形光束：beam_1 ... beam_6, phase = phi_1 ... phi_6
```

网络输入：

```text
7 光束相干合成后的远场光强图像
```

网络输出：

```text
[sin(phi_1), cos(phi_1), ..., sin(phi_6), cos(phi_6)]
```

输出维度为 12，对应 6 个外圈光束的相对相位误差。

## 技术路线

```text
7 光束近场复振幅
↓
FFT 傅里叶传播
↓
远场光强图像
↓
CNN 相位反演网络
↓
6 路相对相位 sin/cos 编码
↓
解码得到相位误差
↓
重建 7 光束近场复振幅
↓
FFT 重建远场光强
↓
相位监督损失 + 远场物理一致性损失
```

总损失函数仍保持：

```text
L_total = L_phase + lambda_phy * L_farfield
```

## 双光束成果的新定位

双光束实验不删除，也不视为废弃工作。它在新项目中承担以下作用：

- 方法可行性证明：证明远场光强中确实包含可学习的相位信息。
- 代码原型：数据生成、训练脚本、相位指标和物理损失都可作为 7 光束实现参考。
- 低维基线：用于说明从 1 个相位变量到 6 个相位变量时任务难度如何增加。
- 消融对照：物理约束、噪声鲁棒性、振幅失配等实验在双光束上已有清晰记录。

## 需要新增或改造的模块

### 1. 7 光束公共仿真模块

建议文件：

```text
simulation/common/multi_beam_core.py
```

计划功能：

- 生成 7 光束六边形阵列坐标。
- 生成多光束高斯近场复振幅。
- 固定中心光束相位为 0。
- 随机采样外圈 6 路相位。
- 输出 12 维 `sin/cos` 标签。
- 通过 FFT 生成远场光强。
- 支持噪声、振幅失配、位置偏移等扰动扩展。

### 2. 7 光束静态数据生成脚本

建议文件：

```text
simulation/static/generate_seven_beam_dataset.py
```

输出：

```text
images_<prefix>.npy
labels_<prefix>.npy
phases_<prefix>.npy
config_<prefix>.json
```

其中：

```text
images: [N, H, W]
labels: [N, 12]
phases: [N, 6]
```

### 3. 7 光束物理一致性模块

建议改造：

```text
train/physics_loss.py
```

或新增：

```text
train/multi_beam_physics_loss.py
```

核心任务是根据 12 维 `sin/cos` 预测结果重建 6 路相位，再构造 7 光束近场并通过 FFT 重建远场。

### 4. 7 光束训练脚本

现有 `SimplePhaseCNN` 已支持调整 `output_dim`，因此可以复用模型结构：

```text
output_dim = 12
```

但训练脚本需要支持：

- 多相位整体 RMSE。
- 逐通道 RMSE。
- 7 光束远场物理一致性损失。
- 7 光束结果图和指标表。

## 后续周期调整

新的近期周期建议为：

### Cycle 11

实现 7 光束基础仿真模块和 smoke 数据集。

### Cycle 12

生成 7 光束主数据集并训练普通 CNN baseline。

### Cycle 13

扩展 7 光束物理一致性损失并训练物理约束 CNN。

### Cycle 14

完成 7 光束 `lambda_phy` 权重消融。

### Cycle 15

完成 7 光束探测器噪声鲁棒性实验。

### Cycle 16

完成 7 光束振幅失配、位置偏移和复杂扰动实验。

## 预期论文表达

论文中可以采用递进结构：

1. 先介绍双光束实验，证明方法可行。
2. 再扩展到 7 光束系统，展示方法对多路高维相位反演的适用性。
3. 最后在噪声和非理想扰动下比较普通 CNN 与物理约束 CNN，突出物理约束带来的鲁棒性和物理可信度。

这样既保留前期工作价值，又能让最终课题更接近“多路相干合成”的工程应用场景。
